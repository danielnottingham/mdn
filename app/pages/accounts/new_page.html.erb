<%= render Application::FrameComponent.new(current_user: current_user, current_controller: controller) do %>
  <%= mdn_main_content(title: t(".header_title")) do %>
    <%= render Accounts::FormComponent.new(account: account, url: accounts_path) %>

    <div class="mt-4">
      <%= mdn_button(builder: :link_to, href: accounts_path, color: :light) { t(".back_to_list") } %>
    </div>
  <% end %>
<% end %>
