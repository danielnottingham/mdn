<main class="h-screen bg-gray-50 p-4 pt-20 md:ml-64">
  <div class="flex justify-between items-center border-b-4 border-indigo-500">
    <h2 class=" text-2xl font-bold"><%= title %></h2>
    <% actions.each do |action| %>
      <%= action %>
    <% end %>
  </div>
  <div class="mt-1 p-4">
    <%= content %>
  </div>
</main>
